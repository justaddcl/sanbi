version: 2.1

executors:
  node-executor:
    docker:
      - image: node:24.1.0

commands:
  setup:
    description: Set up common system expectations
    steps:
      - checkout
      - restore_cache:
          name: Restore pnpm Package Cache
          keys:
            - pnpm-packages-{{ checksum "pnpm-lock.yaml" }}
      - run:
          name: Install pnpm package manager
          command: |
            npm install -g corepack
            corepack enable
            corepack prepare pnpm@latest-9 --activate
            pnpm config set store-dir .pnpm-store
      - run:
          name: Install Dependencies
          command: |
            pnpm install
      - save_cache:
          name: Save pnpm Package Cache
          key: pnpm-packages-{{ checksum "pnpm-lock.yaml" }}
          paths:
            - .pnpm-store

jobs:
  lint:
    executor: node-executor
    steps:
      - setup
      - run:
          name: Lint
          command: |
            pnpm lint

  unit_test:
    executor: node-executor
    steps:
      - setup
      - run:
          name: Test
          command: |
            pnpm test

  e2e_test:
    docker:
      - image: node:24.1.0
        environment:
          DATABASE_URL: postgresql://sanbi@localhost:5432/sanbi_e2e
          POSTGRES_URL: postgresql://sanbi@localhost:5432/sanbi_e2e
          SANBI_E2E: "1"
      - image: cimg/postgres:16.4
        environment:
          POSTGRES_DB: sanbi_e2e
          POSTGRES_USER: sanbi
          POSTGRES_HOST_AUTH_METHOD: trust
    steps:
      - setup
      - run:
          name: Install Playwright browsers
          command: |
            pnpm exec playwright install --with-deps chromium webkit
      - run:
          name: Push E2E schema
          command: |
            DATABASE_URL=postgresql://sanbi@localhost:5432/sanbi_e2e POSTGRES_URL=postgresql://sanbi@localhost:5432/sanbi_e2e pnpm db:push
      - run:
          name: Run E2E tests
          command: |
            DATABASE_URL=postgresql://sanbi@localhost:5432/sanbi_e2e POSTGRES_URL=postgresql://sanbi@localhost:5432/sanbi_e2e pnpm test:e2e:ci
      - store_artifacts:
          path: playwright-report
      - store_artifacts:
          path: test-results

workflows:
  validation:
    jobs:
      - lint
      - unit_test
      - e2e_test
